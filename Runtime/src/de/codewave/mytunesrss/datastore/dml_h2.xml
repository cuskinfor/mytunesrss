<?xml version="1.0" encoding="UTF-8"?>

<statements>

    <statement name="findRandomTracks">
        <sql>SELECT LIMIT 0 :maxCount RAND() AS rnd, {trackColumns} FROM track t ORDER BY rnd</sql>
    </statement>

    <statement name="nextPlaylistId">
        <sql>SELECT TOP 1 NEXT VALUE FOR playlist_id_sequence AS id FROM system_information</sql>
    </statement>

    <!-- reusable sql fragments -->
    <fragment name="tuneDatabaseAfterHelpTablesUpdate">ANALYZE SAMPLE_SIZE 0</fragment>

</statements>