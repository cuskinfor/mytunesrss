<?xml version="1.0" encoding="UTF-8"?>

<statements>

    <statement name="preCreateAllTables"/>

    <statement name="initialize">
        <sql>SELECT COUNT(*) FROM pg_catalog.pg_stat_user_tables WHERE LOWER(schemaname) = 'public' AND LOWER(relname) = 'track'</sql>
    </statement>

    <fragment name="datatype_varchar_ignorecase">VARCHAR</fragment>
    <fragment name="datatype_boolean">BOOLEAN</fragment>
    <fragment name="cached_table_prefix"></fragment>

</statements>