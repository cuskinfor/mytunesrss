<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>de.codewave.mytunesrss</groupId>
    <artifactId>mytunesrss</artifactId>
    <packaging>pom</packaging>
    <version>3.1-SNAPSHOT</version>
    <name>mytunesrss</name>
    <distributionManagement>
        <site>
            <url>/Users/mdescher/Sites/java-projects/mytunesrss</url>
        </site>
    </distributionManagement>
    <inceptionYear>2006</inceptionYear>
    <parent>
        <groupId>de.codewave</groupId>
        <artifactId>codewave-maven2-base</artifactId>
        <version>1.0</version>
    </parent>
    <modules>
        <module>runtime</module>
        <module>webapp</module>
        <module>apple</module>
        <module>shareit-keygen</module>
    </modules>
    <build>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <configuration>
                            <descriptors>
                                <descriptor>src/assembly/system-independent.xml</descriptor>
                                <!--<descriptor>src/assembly/windows.xml</descriptor>-->
                                <!--<descriptor>src/assembly/macosx.xml</descriptor>-->
                            </descriptors>
                        </configuration>
                        <goals>
                            <goal>assembly</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <configuration>
                            <tasks>
                                <unwar src="${outputDirectory}/${artifactId}-${version}.dir/mytunesrss-${version}/webapps/ROOT.war" dest="${outputDirectory}/${artifactId}-${version}.dir/mytunesrss-${version}/webapps/ROOT"/>
                                <delete file="${outputDirectory}/${artifactId}-${version}.dir/mytunesrss-${version}/webapps/ROOT.war"/>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin-->
        </plugins>
    </build>
</project>
